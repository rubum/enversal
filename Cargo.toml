[workspace]
resolver = "2"
members = [
    "core",
    "cli",
    "daemon",
    "sandbox",
    "brain"
]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["Macken <macken@enversal.ai>"]
